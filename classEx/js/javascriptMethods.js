var str = 'this is a string.';

// length method - returns size of the string
var strLen = str.length;

var strIdx = str[8];

// indexOf method - get position of character in string
var strIdxOf = str.indexOf(5);

// slice method - get a substring or specified portion of a string.center
var strSlice = str.slice(5,7);

// toUpperCase() and toLowerCase() - change the case of a string character
var strUpper = str.toUpperCase();
var strLower = str.toLowerCase();

// replace() method = replace character of a string 
var strRpl = str.replace();





console.log(strLen);